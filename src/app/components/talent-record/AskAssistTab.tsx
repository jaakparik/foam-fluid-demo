export function AskAssistTab() {
  return (
    <div
      style={{
        backgroundColor: "var(--filter-card-bg)",
        border: "1px solid var(--border-subtle)",
        borderRadius: "12px",
        padding: "48px",
        textAlign: "center",
      }}
    >
      <h3
        style={{
          color: "var(--table-text-primary)",
          fontSize: "18px",
          fontWeight: "500",
        }}
      >
        Ask Assist
      </h3>
    </div>
  );
}
