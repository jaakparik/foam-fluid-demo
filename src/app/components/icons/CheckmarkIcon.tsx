import { Checkmark } from 'foamicons';

export function CheckmarkIcon() {
  return (
    <Checkmark
      size={20}
      strokeWidth="var(--icon-stroke-width)"
      style={{ color: '#54657D' }}
    />
  );
}
