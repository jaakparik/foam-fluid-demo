import { Star } from 'foamicons';

export function BrandStarIcon() {
  return (
    <Star
      size={20}
      strokeWidth="var(--icon-stroke-width)"
      style={{ color: 'currentColor' }}
    />
  );
}
