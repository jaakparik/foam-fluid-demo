import { FrownDuotone } from 'foamicons';

export function SadIcon() {
  return (
    <FrownDuotone
      size={20}
      strokeWidth="var(--icon-stroke-width)"
      style={{ color: '#54657D' }}
    />
  );
}
